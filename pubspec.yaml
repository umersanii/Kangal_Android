name: kangal

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  drift: ^1.7.0
  path_provider: ^2.0.11
  telephony: ^0.2.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.3
  drift_dev: ^1.7.0